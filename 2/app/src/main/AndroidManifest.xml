<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.layoutlab2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LayoutLab2">

        <!-- 线性布局（启动页） -->
        <activity
            android:name=".LinearActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 其他Activity -->
        <activity android:name=".Constraint1Activity" android:exported="true"/>
        <activity android:name=".TableActivity" android:exported="true"/>
        <activity android:name=".Constraint2Activity" android:exported="true"/>
    </application>

</manifest>